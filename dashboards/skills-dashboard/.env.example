# Optional: enables LLM enrichment (npm run enrich / the Enrich button) for
# skills with no usable description and rows classified as "Other".
OPENROUTER_API_KEY=

# Optional overrides
SKILLS_DASH_PORT=8788
HERMES_HOME=
